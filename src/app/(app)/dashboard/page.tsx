// Placeholder da F1 — só resolve a rota e o título dentro do shell. O conteúdo real
// (KPIs, gráficos, funil) entra depois.
export default function DashboardPage() {
  return (
    <div className="mx-auto w-full max-w-[1400px] px-4 py-6 md:px-8 md:py-8">
      <h1 className="text-2xl font-semibold tracking-tight text-text">Dashboard</h1>
      <p className="mt-1 text-sm text-muted">Visão geral do portal de parceiros.</p>
    </div>
  );
}
